import React from 'react';
import './SideNav.scss';

const SideNav = () => {
    return (
        <aside className="d-side-nav">
            side navigation
        </aside>
    );
};

export default SideNav;